# Condicionais

Os fluxos condicionais são uma parte essencial da programação, permitindo que os desenvolvedores criem lógicas dinâmicas e adaptáveis, dependendo de condições específicas. Este documento explora detalhadamente o conceito de fluxos condicionais em JavaScript.

## **Sumário**

1. [O que é e para que serve?](https://chatgpt.com/c/677fba4f-1a58-8003-b311-a5ba4637e315#o-que-%C3%A9-e-para-que-serve)
2. [Como funciona?](https://chatgpt.com/c/677fba4f-1a58-8003-b311-a5ba4637e315#como-funciona)
3. [Quando utilizar?](https://chatgpt.com/c/677fba4f-1a58-8003-b311-a5ba4637e315#quando-utilizar)
4. [Sintaxe de uso detalhada](https://chatgpt.com/c/677fba4f-1a58-8003-b311-a5ba4637e315#sintaxe-de-uso-detalhada)
    - `if/else`
    - `switch`
    - Operadores ternários
5. [Restrições de uso](https://chatgpt.com/c/677fba4f-1a58-8003-b311-a5ba4637e315#restri%C3%A7%C3%B5es-de-uso)
6. [Exemplos Práticos](https://chatgpt.com/c/677fba4f-1a58-8003-b311-a5ba4637e315#exemplos-pr%C3%A1ticos)
7. [Conclusão](https://chatgpt.com/c/677fba4f-1a58-8003-b311-a5ba4637e315#conclus%C3%A3o)

---

## **O que é e para que serve?**

### **Definição**

Fluxos condicionais são estruturas que permitem executar diferentes blocos de código com base em condições avaliadas como verdadeiras (`true`) ou falsas (`false`). Eles formam a base do controle de fluxo em qualquer programa, ajudando a decidir qual ação tomar dependendo de entradas, estados ou resultados.

### **Finalidade**

- **Tomada de Decisões**: Escolher qual código executar com base em condições.
- **Automação de Processos**: Automatizar respostas ou comportamentos específicos.
- **Lógica Dinâmica**: Adaptar o comportamento do programa a diferentes situações.

---

## **Como funciona?**

Fluxos condicionais verificam expressões lógicas ou valores booleanos e executam blocos de código específicos com base no resultado.

O **princípio básico** é:

1. Avaliar uma condição.
2. Determinar se ela é verdadeira (`true`) ou falsa (`false`).
3. Executar o bloco de código correspondente.

Por exemplo:

```jsx
const temperature = 30;

if (temperature > 25) {
  console.log("Está quente!");
} else {
  console.log("Está frio.");
}

```

### Fluxo de Trabalho:

1. Avaliar `temperature > 25`.
2. Se for `true`, executar `console.log("Está quente!")`.
3. Caso contrário (`false`), executar `console.log("Está frio.")`.

---

## **Quando utilizar?**

Os fluxos condicionais devem ser utilizados quando o programa precisa reagir de formas diferentes com base em:

- **Entradas do Usuário**: Ex.: validar formulários.
- **Estado do Sistema**: Ex.: verificar se um recurso está disponível.
- **Valores Dinâmicos**: Ex.: calcular descontos dependendo do valor de compra.

**Exemplo Real**:

```jsx
const age = 18;

if (age >= 18) {
  console.log("Você pode dirigir.");
} else {
  console.log("Você ainda não pode dirigir.");
}

```

---

## **Sintaxe de Uso Detalhada**

### **1. Estrutura `if/else`**

A estrutura `if/else` é a base de todos os fluxos condicionais. Permite testar uma ou mais condições sequencialmente.

### **Sintaxe:**

```jsx
if (condição) {
  // Bloco de código se a condição for verdadeira
} else {
  // Bloco de código se a condição for falsa
}

```

### **Exemplo Simples:**

```jsx
const isSunny = true;

if (isSunny) {
  console.log("Vamos ao parque!");
} else {
  console.log("Ficaremos em casa.");
}

```

### **Exemplo com `else if`:**

```jsx
const score = 85;

if (score >= 90) {
  console.log("Nota A");
} else if (score >= 75) {
  console.log("Nota B");
} else if (score >= 50) {
  console.log("Nota C");
} else {
  console.log("Nota F");
}

```

### **Fluxo de Trabalho:**

1. A condição do `if` é avaliada primeiro.
2. Se `false`, o próximo `else if` (se existir) é avaliado.
3. Se nenhuma condição for verdadeira, o bloco `else` será executado.

---

### **2. Estrutura `switch`**

A estrutura `switch` é usada para testar múltiplas condições de igualdade. É útil quando se deseja comparar um valor específico contra várias possibilidades.

### **Sintaxe:**

```jsx
switch (expressão) {
  case valor1:
    // Bloco de código se a expressão === valor1
    break;
  case valor2:
    // Bloco de código se a expressão === valor2
    break;
  default:
    // Bloco de código se nenhuma condição for satisfeita
}

```

### **Exemplo Simples:**

```jsx
const day = "Monday";

switch (day) {
  case "Monday":
    console.log("Início da semana.");
    break;
  case "Friday":
    console.log("Quase fim de semana!");
    break;
  default:
    console.log("Dia comum.");
}

```

### **Importante:**

- Sempre use `break` para evitar que as condições subsequentes sejam executadas.
- O bloco `default` é opcional, mas recomendado.

---

### **3. Operador Ternário**

O operador ternário é uma forma compacta de escrever condições simples.

### **Sintaxe:**

```jsx
condição ? valorSeVerdadeiro : valorSeFalso;

```

### **Exemplo:**

```jsx
const age = 20;
const message = age >= 18 ? "Maior de idade" : "Menor de idade";
console.log(message);

```

---

## **Restrições de Uso**

1. **Complexidade Excessiva**:
    
    Usar muitos `if/else` ou `switch` encadeados pode dificultar a leitura do código.
    
2. **Evitar Condições Redundantes**:
    
    Exemplo:
    
    ```jsx
    if (isSunny === true) {
      console.log("Está ensolarado.");
    }
    
    ```
    
    **Correção:**
    
    ```jsx
    if (isSunny) {
      console.log("Está ensolarado.");
    }
    
    ```
    
3. **Cuidado com Tipos de Comparação**:
    
    Sempre prefira comparações estritas (`===`) para evitar resultados inesperados devido à coerção de tipos.
    
4. **Manutenção**:
    
    Para cenários complexos, considere usar padrões de design como **Strategy Pattern** em vez de muitos `if/else`.
    

---

## **Exemplos Práticos**

### **Validação de Login**

```jsx
const username = "admin";
const password = "12345";

if (username === "admin" && password === "12345") {
  console.log("Login bem-sucedido!");
} else {
  console.log("Credenciais incorretas.");
}

```

### **Recomendação de Roupa**

```jsx
const temperature = 15;

if (temperature < 10) {
  console.log("Use um casaco.");
} else if (temperature < 20) {
  console.log("Use uma jaqueta leve.");
} else {
  console.log("Vista roupas confortáveis.");
}

```

---

## **Conclusão**

Os fluxos condicionais em JavaScript são essenciais para criar programas dinâmicos que reagem de acordo com diferentes entradas e condições. Eles fornecem a base para lógica complexa, adaptando-se a quase todas as necessidades no desenvolvimento.

### **Resumo das Estruturas:**

- **`if/else`**: Ideal para fluxos condicionais simples e hierárquicos.
- **`switch`**: Melhor para comparar uma única expressão contra múltiplos valores.
- **Operador Ternário**: Alternativa compacta para condições simples.

### **Boas Práticas:**

- Evite complicar os fluxos condicionais; refatore códigos longos.
- Prefira comparações estritas (`===`) para maior segurança.
- Sempre pense na legibilidade e mantenha o código organizado.

Com esses conhecimentos, você está pronto para implementar lógicas condicionais eficazes em seus projetos JavaScript! 🚀