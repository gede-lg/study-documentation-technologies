# Eventos DOM

# Lista de Eventos para `addEventListener` no DOM

Esta lista apresenta os principais eventos que podem ser usados com `addEventListener` em elementos HTML, organizados por categoria e com descrições resumidas.

---

## 🖱️ Eventos de Mouse

| Evento | Descrição |
| --- | --- |
| click | Disparado quando o botão do mouse é clicado. |
| dblclick | Disparado quando há um duplo clique com o mouse. |
| mousedown | Disparado quando o botão do mouse é pressionado. |
| mouseup | Disparado quando o botão do mouse é liberado. |
| mouseenter | Disparado quando o ponteiro entra na área de um elemento (sem filhos). |
| mouseleave | Disparado quando o ponteiro sai da área de um elemento (sem filhos). |
| mousemove | Disparado quando o ponteiro se move dentro do elemento. |
| mouseover | Disparado quando o ponteiro passa sobre o elemento (inclui filhos). |
| mouseout | Disparado quando o ponteiro sai do elemento (inclui filhos). |
| contextmenu | Disparado quando o botão direito do mouse é clicado. |

---

## ⌨️ Eventos de Teclado

| Evento | Descrição |
| --- | --- |
| keydown | Disparado quando uma tecla é pressionada. |
| keyup | Disparado quando uma tecla é liberada. |
| keypress | Disparado enquanto a tecla é mantida pressionada (obsoleto em alguns navegadores). |

---

## 🧾 Eventos de Formulários

| Evento | Descrição |
| --- | --- |
| input | Disparado quando o valor de um `<input>`, `<textarea>` ou `<select>` muda. |
| change | Disparado quando um valor é alterado e o foco sai do campo. |
| focus | Disparado quando um elemento recebe o foco. |
| blur | Disparado quando um elemento perde o foco. |
| submit | Disparado ao enviar um formulário. |
| reset | Disparado ao resetar um formulário. |
| invalid | Disparado quando um campo de formulário inválido é validado. |
| select | Disparado quando texto dentro de `<input>` ou `<textarea>` é selecionado. |

---

## 🖲️ Eventos de Ponteiro

| Evento | Descrição |
| --- | --- |
| pointerdown | Disparado quando um ponteiro (mouse, caneta, dedo) é pressionado. |
| pointerup | Disparado quando o ponteiro é liberado. |
| pointermove | Disparado quando o ponteiro se move. |
| pointerenter | Disparado quando o ponteiro entra na área de um elemento (sem propagar filhos). |
| pointerleave | Disparado quando o ponteiro sai da área de um elemento (sem propagar filhos). |
| pointerover | Disparado quando o ponteiro entra no elemento (propaga filhos). |
| pointerout | Disparado quando o ponteiro sai do elemento (propaga filhos). |
| pointercancel | Disparado quando o ponteiro é interrompido, como por uma rotação de tela. |

---

## 🤏 Eventos de Toque (Touch - Mobile)

| Evento | Descrição |
| --- | --- |
| touchstart | Disparado quando um dedo toca a tela. |
| touchend | Disparado quando o dedo é removido da tela. |
| touchmove | Disparado quando o dedo se move sobre a tela. |
| touchcancel | Disparado quando o toque é cancelado (por exemplo, por uma interrupção). |

---

## 🪟 Eventos da Janela/Documento

| Evento | Descrição |
| --- | --- |
| load | Disparado quando a página ou recurso termina de carregar. |
| unload | Disparado ao sair da página. |
| beforeunload | Disparado antes da página ser descarregada. |
| resize | Disparado quando a janela é redimensionada. |
| scroll | Disparado ao rolar a página ou um elemento com scroll. |

---

## 🎯 Eventos de Foco

| Evento | Descrição |
| --- | --- |
| focus | Disparado quando um elemento recebe foco. |
| blur | Disparado quando um elemento perde o foco. |
| focusin | Similar ao focus, mas propaga (bubbling). |
| focusout | Similar ao blur, mas propaga (bubbling). |

---

## 📤 Eventos de Drag & Drop

| Evento | Descrição |
| --- | --- |
| drag | Disparado durante o movimento de um item. |
| dragstart | Disparado quando começa o arrasto. |
| dragend | Disparado quando termina o arrasto. |
| dragenter | Disparado quando um item arrastado entra na área de destino. |
| dragleave | Disparado quando sai da área de destino. |
| dragover | Disparado continuamente enquanto o item está sobre o destino. |
| drop | Disparado quando o item é solto no destino. |

---

## 📋 Eventos de Clipboard

| Evento | Descrição |
| --- | --- |
| copy | Disparado quando o conteúdo é copiado. |
| cut | Disparado quando o conteúdo é recortado. |
| paste | Disparado quando conteúdo é colado. |

---

## 🎵 Eventos de Mídia

| Evento | Descrição |
| --- | --- |
| play | Disparado quando a reprodução começa. |
| pause | Disparado quando a reprodução é pausada. |
| ended | Disparado quando a mídia termina. |
| volumechange | Disparado quando o volume muda. |
| timeupdate | Disparado quando a posição de reprodução muda. |
| seeking | Disparado quando o usuário inicia uma busca na mídia. |
| seeked | Disparado quando a busca termina. |
| canplay | A mídia está pronta para começar a ser reproduzida. |
| canplaythrough | A mídia pode ser reproduzida até o fim sem parar para buffering. |
| durationchange | Disparado quando a duração muda. |
| loadeddata | Dados iniciais foram carregados. |
| loadedmetadata | Metadados como duração e dimensões foram carregados. |
| loadstart | Início do carregamento da mídia. |
| progress | Disparado periodicamente durante o carregamento da mídia. |
| stalled | O navegador não consegue carregar mídia temporariamente. |
| suspend | Carregamento suspenso. |
| waiting | Reprodução pausada enquanto espera por dados. |

---

## 🌐 Eventos de Armazenamento/Rede

| Evento | Descrição |
| --- | --- |
| storage | Disparado quando `localStorage` ou `sessionStorage` são alterados. |
| online | Disparado quando a conexão é restabelecida. |
| offline | Disparado quando a conexão é perdida. |
| error | Disparado quando ocorre um erro de carregamento. |

---

## 🧩 Eventos Customizados

| Evento Customizado | Descrição |
| --- | --- |
| dispatchEvent(...) | Permite criar e escutar eventos personalizados com `new CustomEvent`. |