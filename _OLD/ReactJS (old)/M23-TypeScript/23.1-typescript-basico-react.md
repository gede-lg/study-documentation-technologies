# TypeScript Básico no React

## 🎯 Conceitos Fundamentais

TypeScript adiciona **tipagem estática** ao React, permitindo **detecção de erros em tempo de desenvolvimento**, **autocomplete inteligente**, e **refatoração segura**. Transforma código de **propenso a erros em runtime** para **validado em compile-time**.

## 📋 Setup Básico

```json
// tsconfig.json
{
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true
  }
}
```

## 🔍 Tipagem de Props

### Interface vs Type

```tsx
// Interface (preferida para objetos)
interface ButtonProps {
  label: string;
  onClick: () => void;
  disabled?: boolean;
}

// Type (preferida para unions/primitivos)
type Status = 'idle' | 'loading' | 'success' | 'error';
type ButtonSize = 'small' | 'medium' | 'large';
```

### Componente com Props

```tsx
interface UserCardProps {
  name: string;
  age: number;
  email?: string; // Opcional
  onEdit: (id: number) => void;
}

function UserCard({ name, age, email, onEdit }: UserCardProps) {
  return (
    <div>
      <h2>{name}</h2>
      <p>Age: {age}</p>
      {email && <p>Email: {email}</p>}
      <button onClick={() => onEdit(1)}>Edit</button>
    </div>
  );
}
```

### Props com Children

```tsx
interface ContainerProps {
  title: string;
  children: React.ReactNode; // Aceita qualquer coisa renderizável
}

function Container({ title, children }: ContainerProps) {
  return (
    <div>
      <h1>{title}</h1>
      {children}
    </div>
  );
}
```

## 🎯 Tipagem de Hooks

### useState

```tsx
// Inferência automática
const [count, setCount] = useState(0); // number

// Tipo explícito
const [user, setUser] = useState<User | null>(null);

// Com interface
interface User {
  id: number;
  name: string;
}

const [user, setUser] = useState<User>({
  id: 1,
  name: 'John'
});
```

### useRef

```tsx
// Ref para DOM
const inputRef = useRef<HTMLInputElement>(null);

// Ref para valor mutável
const countRef = useRef<number>(0);

// Uso
function Component() {
  const inputRef = useRef<HTMLInputElement>(null);

  const handleFocus = () => {
    inputRef.current?.focus();
  };

  return <input ref={inputRef} />;
}
```

### useEffect

```tsx
// Sem tipagem extra necessária
useEffect(() => {
  const timer = setTimeout(() => {
    console.log('Hello');
  }, 1000);

  return () => clearTimeout(timer);
}, []);
```

## 🔍 Eventos

```tsx
// onClick
const handleClick = (e: React.MouseEvent<HTMLButtonElement>) => {
  console.log(e.currentTarget);
};

// onChange
const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {
  console.log(e.target.value);
};

// onSubmit
const handleSubmit = (e: React.FormEvent<HTMLFormElement>) => {
  e.preventDefault();
};

// Componente completo
function Form() {
  const handleSubmit = (e: React.FormEvent<HTMLFormElement>) => {
    e.preventDefault();
    const formData = new FormData(e.currentTarget);
  };

  const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    console.log(e.target.value);
  };

  return (
    <form onSubmit={handleSubmit}>
      <input onChange={handleChange} />
      <button type="submit">Submit</button>
    </form>
  );
}
```

## 🎯 ReactNode vs JSX.Element

```tsx
type ReactNode =
  | React.ReactElement
  | string
  | number
  | boolean
  | null
  | undefined;

// ReactNode - aceita tudo renderizável
interface Props {
  children: React.ReactNode;
}

// JSX.Element - apenas elementos React
interface Props {
  icon: JSX.Element;
}

// Uso
<Container>
  Hello {/* string - ok com ReactNode */}
  <div>World</div> {/* element - ok */}
</Container>
```

## ⚠️ React.FC - Por Que Evitar

```tsx
// ❌ Evitar React.FC
const Component: React.FC<Props> = ({ children }) => {
  return <div>{children}</div>;
};

// ✅ Preferir
function Component({ children }: Props) {
  return <div>{children}</div>;
}

// Razões:
// 1. React.FC adiciona children automaticamente (confuso)
// 2. Não funciona bem com generics
// 3. Verbose desnecessariamente
```

## 🚀 Conclusão

TypeScript Básico no React fornece:
- **Tipagem de Props**: Interface/Type
- **Hooks Tipados**: useState, useRef, useEffect
- **Eventos Tipados**: MouseEvent, ChangeEvent, FormEvent
- **Children**: ReactNode vs JSX.Element
- **Evitar**: React.FC

Fundação essencial para código React type-safe e manutenível.
