# TypeScript Intermediário no React

## 🎯 Componentes Genéricos

```tsx
interface ListProps<T> {
  items: T[];
  renderItem: (item: T) => React.ReactNode;
}

function List<T>({ items, renderItem }: ListProps<T>) {
  return (
    <ul>
      {items.map((item, index) => (
        <li key={index}>{renderItem(item)}</li>
      ))}
    </ul>
  );
}

// Uso
interface User {
  id: number;
  name: string;
}

<List<User>
  items={users}
  renderItem={(user) => <span>{user.name}</span>}
/>
```

## 📋 Utility Types

### Partial

```tsx
interface User {
  id: number;
  name: string;
  email: string;
}

// Todos campos opcionais
function updateUser(id: number, updates: Partial<User>) {
  // updates pode ter apenas { name: 'John' }
}
```

### Pick

```tsx
// Selecionar apenas campos específicos
type UserPreview = Pick<User, 'id' | 'name'>;
// { id: number; name: string; }
```

### Omit

```tsx
// Omitir campos específicos
type UserWithoutEmail = Omit<User, 'email'>;
// { id: number; name: string; }
```

### Required

```tsx
interface Config {
  host?: string;
  port?: number;
}

type RequiredConfig = Required<Config>;
// { host: string; port: number; }
```

## 🔍 ComponentProps

```tsx
// Extrair props de componente nativo
type ButtonProps = React.ComponentProps<'button'>;
// Tem onClick, disabled, type, etc.

// Estender props nativos
interface CustomButtonProps extends React.ComponentProps<'button'> {
  variant: 'primary' | 'secondary';
}

function CustomButton({ variant, ...props }: CustomButtonProps) {
  return <button className={variant} {...props} />;
}
```

## 🎯 Discriminated Unions

```tsx
type AsyncState<T> =
  | { status: 'idle' }
  | { status: 'loading' }
  | { status: 'success'; data: T }
  | { status: 'error'; error: string };

function DataComponent() {
  const [state, setState] = useState<AsyncState<User>>({
    status: 'idle'
  });

  if (state.status === 'loading') {
    return <Loading />;
  }

  if (state.status === 'error') {
    return <Error message={state.error} />; // error disponível
  }

  if (state.status === 'success') {
    return <UserCard user={state.data} />; // data disponível
  }

  return <div>Idle</div>;
}
```

## 🔍 Index Signatures

```tsx
interface Dictionary {
  [key: string]: string;
}

const translations: Dictionary = {
  hello: 'Olá',
  goodbye: 'Tchau'
};

// Com generics
interface Map<T> {
  [key: string]: T;
}

const userMap: Map<User> = {
  '1': { id: 1, name: 'John' },
  '2': { id: 2, name: 'Jane' }
};
```

## 🎯 Tipos Nativos React

```tsx
// HTMLAttributes
type DivProps = React.HTMLAttributes<HTMLDivElement>;

// CSSProperties
const styles: React.CSSProperties = {
  color: 'red',
  fontSize: 16
};

// PropsWithChildren
type Props = React.PropsWithChildren<{
  title: string;
}>;
// Equivalente a: { title: string; children?: ReactNode }
```

## 🚀 Conclusão

TypeScript Intermediário adiciona:
- **Generics**: Componentes reutilizáveis
- **Utility Types**: Partial, Pick, Omit, Required
- **ComponentProps**: Extrair tipos nativos
- **Discriminated Unions**: Type-safe state
- **Index Signatures**: Objetos dinâmicos

Ferramentas para código mais flexível e type-safe.
