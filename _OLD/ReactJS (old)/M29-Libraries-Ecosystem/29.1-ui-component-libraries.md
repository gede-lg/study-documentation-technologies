# UI Component Libraries

## 🎯 Introdução e Definição

### Definição Conceitual

**UI Component Libraries** são **coleções pré-construídas** de componentes React reutilizáveis (buttons, inputs, modals, etc), oferecendo **design consistente**, **acessibilidade integrada** e **desenvolvimento acelerado**. Conceitualmente, representam **toolkit de UI** - ao invés de construir botões do zero, usar componentes battle-tested que seguem best practices.

### Problema que Resolve

**Sem Component Library**:
- Reinventar botões, inputs, modals
- Inconsistência de design
- Bugs de acessibilidade
- Tempo desperdiçado em UI básica

**Com Component Library**:
- Componentes prontos e testados
- Design system consistente
- Acessibilidade built-in
- Foco em features de negócio

---

## 🎨 Material-UI (MUI)

### Instalação

```bash
npm install @mui/material @emotion/react @emotion/styled
```

### Componentes Básicos

```tsx
import {
  Button,
  TextField,
  Card,
  CardContent,
  Typography,
  Box,
  Stack
} from '@mui/material';

function LoginForm() {
  return (
    <Card sx={{ maxWidth: 400, mx: 'auto', mt: 4 }}>
      <CardContent>
        <Typography variant="h5" component="h1" gutterBottom>
          Login
        </Typography>

        <Stack spacing={2}>
          <TextField
            label="Email"
            type="email"
            fullWidth
            required
          />

          <TextField
            label="Password"
            type="password"
            fullWidth
            required
          />

          <Button
            variant="contained"
            size="large"
            fullWidth
          >
            Sign In
          </Button>
        </Stack>
      </CardContent>
    </Card>
  );
}
```

### Theme Customization

```tsx
import { createTheme, ThemeProvider } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2'
    },
    secondary: {
      main: '#dc004e'
    }
  },
  typography: {
    fontFamily: 'Inter, sans-serif',
    h1: {
      fontSize: '2.5rem',
      fontWeight: 700
    }
  },
  components: {
    MuiButton: {
      styleOverrides: {
        root: {
          borderRadius: 8,
          textTransform: 'none'
        }
      }
    }
  }
});

function App() {
  return (
    <ThemeProvider theme={theme}>
      <YourApp />
    </ThemeProvider>
  );
}
```

**Pros**:
- ✅ Design completo (Material Design)
- ✅ Componentes robustos
- ✅ Boa documentação
- ✅ Grande comunidade

**Cons**:
- ❌ Bundle size grande (~300KB)
- ❌ Design opinado (Material)
- ❌ Customização pode ser verbosa

---

## 🎯 Chakra UI

### Instalação

```bash
npm install @chakra-ui/react @emotion/react @emotion/styled framer-motion
```

### Componentes Básicos

```tsx
import {
  Box,
  Button,
  Input,
  VStack,
  Heading,
  FormControl,
  FormLabel
} from '@chakra-ui/react';

function LoginForm() {
  return (
    <Box maxW="md" mx="auto" mt={8} p={6} borderWidth={1} borderRadius="lg">
      <VStack spacing={4} align="stretch">
        <Heading size="lg">Login</Heading>

        <FormControl>
          <FormLabel>Email</FormLabel>
          <Input type="email" />
        </FormControl>

        <FormControl>
          <FormLabel>Password</FormLabel>
          <Input type="password" />
        </FormControl>

        <Button colorScheme="blue" size="lg" width="full">
          Sign In
        </Button>
      </VStack>
    </Box>
  );
}
```

### Theme Customization

```tsx
import { extendTheme, ChakraProvider } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    brand: {
      50: '#e3f2fd',
      100: '#bbdefb',
      500: '#2196f3',
      900: '#0d47a1'
    }
  },
  fonts: {
    heading: 'Inter, sans-serif',
    body: 'Inter, sans-serif'
  },
  components: {
    Button: {
      baseStyle: {
        fontWeight: 'semibold',
        borderRadius: 'md'
      },
      variants: {
        solid: {
          bg: 'brand.500',
          color: 'white'
        }
      }
    }
  }
});

function App() {
  return (
    <ChakraProvider theme={theme}>
      <YourApp />
    </ChakraProvider>
  );
}
```

**Pros**:
- ✅ Excelente DX (Developer Experience)
- ✅ Acessibilidade built-in
- ✅ Composable (flexível)
- ✅ Dark mode fácil

**Cons**:
- ❌ Bundle size médio
- ❌ Menos componentes que MUI

---

## 🎨 shadcn/ui

### Instalação

```bash
npx shadcn-ui@latest init
```

### Conceito Único

```
Outras libraries: npm install
shadcn/ui: COPIA componentes para seu projeto

Vantagens:
✅ Ownership completo (código no seu repo)
✅ Customização total
✅ Tree-shakeable (apenas componentes usados)
✅ Zero dependências extras
```

### Adicionar Componentes

```bash
npx shadcn-ui@latest add button
npx shadcn-ui@latest add input
npx shadcn-ui@latest add card
```

### Uso

```tsx
// Copiado para: src/components/ui/button.tsx
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

function LoginForm() {
  return (
    <Card className="max-w-md mx-auto mt-8">
      <CardHeader>
        <CardTitle>Login</CardTitle>
      </CardHeader>
      <CardContent className="space-y-4">
        <Input type="email" placeholder="Email" />
        <Input type="password" placeholder="Password" />
        <Button className="w-full">Sign In</Button>
      </CardContent>
    </Card>
  );
}
```

**Pros**:
- ✅ Ownership (código seu)
- ✅ Customização total
- ✅ Tree-shakeable
- ✅ Baseado em Radix UI (acessibilidade)

**Cons**:
- ❌ Updates manuais
- ❌ Menos componentes

---

## 🧩 Headless UI (Tailwind)

### Instalação

```bash
npm install @headlessui/react
```

### Conceito

```
Headless UI: Lógica + Acessibilidade
Você: Estilização (Tailwind CSS)

Vantagens:
✅ Máxima flexibilidade
✅ Acessibilidade garantida
✅ Design customizado
```

### Componentes

```tsx
import { Dialog, Transition } from '@headlessui/react';
import { Fragment, useState } from 'react';

function Modal() {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <>
      <button
        onClick={() => setIsOpen(true)}
        className="bg-blue-500 text-white px-4 py-2 rounded"
      >
        Open Modal
      </button>

      <Transition show={isOpen} as={Fragment}>
        <Dialog onClose={() => setIsOpen(false)}>
          <Transition.Child
            as={Fragment}
            enter="ease-out duration-300"
            enterFrom="opacity-0"
            enterTo="opacity-100"
            leave="ease-in duration-200"
            leaveFrom="opacity-100"
            leaveTo="opacity-0"
          >
            <div className="fixed inset-0 bg-black/30" />
          </Transition.Child>

          <div className="fixed inset-0 flex items-center justify-center p-4">
            <Transition.Child
              as={Fragment}
              enter="ease-out duration-300"
              enterFrom="opacity-0 scale-95"
              enterTo="opacity-100 scale-100"
              leave="ease-in duration-200"
              leaveFrom="opacity-100 scale-100"
              leaveTo="opacity-0 scale-95"
            >
              <Dialog.Panel className="bg-white rounded-lg p-6 max-w-md">
                <Dialog.Title className="text-lg font-bold">
                  Modal Title
                </Dialog.Title>

                <p className="mt-2">Modal content goes here</p>

                <button
                  onClick={() => setIsOpen(false)}
                  className="mt-4 bg-blue-500 text-white px-4 py-2 rounded"
                >
                  Close
                </button>
              </Dialog.Panel>
            </Transition.Child>
          </div>
        </Dialog>
      </Transition>
    </>
  );
}
```

**Pros**:
- ✅ Flexibilidade máxima
- ✅ Acessibilidade built-in
- ✅ Bundle pequeno

**Cons**:
- ❌ Precisa estilizar tudo
- ❌ Poucos componentes

---

## 🎯 Comparação

```
Library      | Bundle Size | Customização | A11y | Learning Curve
-------------|-------------|--------------|------|---------------
MUI          | 🔴 Grande   | 🟡 Média     | ✅   | 🟡 Média
Chakra UI    | 🟡 Média    | ✅ Alta      | ✅   | ✅ Fácil
shadcn/ui    | ✅ Pequena  | ✅ Total     | ✅   | 🟡 Média
Headless UI  | ✅ Pequena  | ✅ Total     | ✅   | 🔴 Difícil
Ant Design   | 🔴 Grande   | 🟡 Média     | ✅   | 🟡 Média
```

---

## 🚀 Recomendações

### Quando Usar Cada Uma

```
MUI (Material-UI):
✅ Precisa de design completo
✅ Admin panels
✅ Dashboard apps
✅ Rapidez > Customização
❌ Landing pages (bundle grande)

Chakra UI:
✅ Melhor DX
✅ Customização frequente
✅ Dark mode
✅ Modern apps
❌ Precisa Material Design

shadcn/ui:
✅ Ownership de código
✅ Customização total
✅ Bundle size critical
✅ Tailwind CSS user
❌ Precisa muitos componentes prontos

Headless UI:
✅ Design system único
✅ Máxima flexibilidade
✅ Apenas acessibilidade
❌ Não quer estilizar tudo
```

---

## 🚀 Conclusão

UI Component Libraries incluem:
- **MUI**: Completo, Material Design, grande bundle
- **Chakra UI**: Excelente DX, composable, acessível
- **shadcn/ui**: Ownership, customização total, Tailwind
- **Headless UI**: Headless (lógica + a11y), você estiliza

**Escolha baseado em**:
- 🎨 Design: Material? → MUI | Custom? → shadcn/Headless
- 📦 Bundle: Critical? → shadcn/Headless | OK? → Chakra/MUI
- ⚡ Velocity: Rápido? → MUI/Chakra | Custom? → shadcn/Headless
- 🎯 DX: Melhor? → Chakra | Flexível? → shadcn

Não existe "melhor" - apenas "melhor para seu caso"!
