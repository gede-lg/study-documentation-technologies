# Custom Hooks: Timing & Lifecycle

## 🎯 Introdução e Definição

### Definição Conceitual

**Custom Hooks de Timing & Lifecycle** são hooks especializados em **controle temporal** e **ciclo de vida de componentes** - intervals controlados (useInterval), timeouts gerenciados (useTimeout), execução apenas em updates (useUpdateEffect), e detecção de primeira render (useIsFirstRender). Representam padrões de **timing e fases de componente** que facilitam lógica temporal sem bugs de cleanup.

Esses hooks abstraem complexidades de setInterval/setTimeout (cleanup, stale closures, referências), e lifecycle de componentes (mount vs update, primeira vs subsequentes renders). Transformam código imperativo propenso a erros em **APIs declarativas seguras** - ao invés de gerenciar clearInterval manualmente, você escreve `useInterval(callback, delay)`.

**Filosofia:** Timing é **difícil em React** - closures capturam valores antigos, cleanup esquecido causa leaks, primeira render precisa tratamento especial. Hooks de timing/lifecycle resolvem esses problemas de uma vez por todas.

### Contexto Histórico e Motivação

**Evolução dos patterns de timing:**

**2013-2018: Classes + Lifecycle**
```javascript
class Timer extends React.Component {
  componentDidMount() {
    this.interval = setInterval(() => {
      this.setState({ count: this.state.count + 1 });
    }, 1000);
  }

  componentWillUnmount() {
    clearInterval(this.interval); // Fácil esquecer!
  }
}
```

**2018-2019: Hooks + Problemas**
```javascript
function Timer() {
  const [count, setCount] = useState(0);

  useEffect(() => {
    const interval = setInterval(() => {
      console.log(count); // ❌ Sempre loga 0 (stale closure!)
    }, 1000);

    return () => clearInterval(interval);
  }, []); // Deps incorretas
}
```

**Problema:** Stale closures, deps confusas, cleanup esquecido.

**2019-presente: Custom Hooks**
Dan Abramov escreveu artigo famoso ["Making setInterval Declarative with React Hooks"](https://overreacted.io/making-setinterval-declarative-with-react-hooks/) mostrando useInterval como solução.

```javascript
function Timer() {
  const [count, setCount] = useState(0);

  useInterval(() => {
    setCount(count + 1); // ✓ Sempre tem valor atual
  }, 1000);
}
```

**Motivação:** Encapsular patterns de timing que são sutilmente bugados quando feitos manualmente.

### Problema Fundamental que Resolve

**Sem custom hooks de timing/lifecycle:**

```javascript
function Component() {
  const [count, setCount] = useState(0);
  const [data, setData] = useState(null);

  // ❌ Interval com stale closure
  useEffect(() => {
    const interval = setInterval(() => {
      console.log(count); // Sempre loga 0!
    }, 1000);
    return () => clearInterval(interval);
  }, []); // count não nas deps

  // ❌ Timeout esquece cleanup
  useEffect(() => {
    setTimeout(() => {
      setData('loaded');
    }, 2000);
    // Faltou cleanup! Se desmontar antes, setState em componente unmounted
  }, []);

  // ❌ Detectar mount vs update (verbose)
  const isFirstRender = useRef(true);
  useEffect(() => {
    if (isFirstRender.current) {
      isFirstRender.current = false;
      return; // Skip no mount
    }
    console.log('Update!');
  });

  // ❌ Executar apenas em updates (não mount)
  const didMountRef = useRef(false);
  useEffect(() => {
    if (!didMountRef.current) {
      didMountRef.current = true;
      return;
    }
    // Lógica de update
  }, [dependency]);
}
```

**Com custom hooks:**

```javascript
function Component() {
  const [count, setCount] = useState(0);

  useInterval(() => {
    console.log(count); // ✓ Sempre atual
  }, 1000);

  useTimeout(() => {
    setData('loaded'); // ✓ Cleanup automático
  }, 2000);

  const isFirstRender = useIsFirstRender();

  useUpdateEffect(() => {
    console.log('Update!'); // ✓ Apenas em updates
  }, [dependency]);
}
```

### Importância no Ecossistema

Hooks de timing/lifecycle são **essenciais** para:

- **Timers seguros**: Interval/timeout sem stale closures
- **Lifecycle preciso**: Distinguir mount/update
- **Cleanup automático**: Prevenir memory leaks
- **Código limpo**: Menos boilerplate de refs

---

## 📋 Sumário Conceitual

### Hooks Cobertos

1. **useInterval**: setInterval declarativo
2. **useTimeout**: setTimeout com cleanup
3. **useUpdateEffect**: useEffect apenas em updates
4. **useIsFirstRender**: Detectar primeira render

### Aspectos Teóricos Centrais

- **Stale closures**: Capturar valores atuais
- **Cleanup**: Limpar timers automaticamente
- **Lifecycle**: Distinguir mount/update
- **Refs**: Armazenar valores mutáveis
- **Callbacks**: Manter handlers atualizados

---

## 🧠 Fundamentos Teóricos

### Pattern: Interval Declarativo

**Problema com setInterval:**
```javascript
// ❌ Callback captura count inicial
const interval = setInterval(() => {
  console.log(count); // Sempre 0
}, 1000);
```

**Solução:** Ref para callback + deps.

### Pattern: Timeout com Cleanup

**Problema:** setTimeout sem cleanup causa setState em unmounted component.

**Solução:** Ref para timer + cleanup em useEffect.

### Pattern: Mount vs Update

**Mount:** Primeira execução do componente.
**Update:** Re-renders subsequentes.

**Necessidade:** Executar lógica diferente em cada fase.

---

## 🔍 Análise Conceitual Profunda

### 8.4.1 useInterval: setInterval Declarativo

```javascript
function useInterval(callback, delay) {
  const savedCallback = useRef(callback);

  // Atualizar ref quando callback muda
  useEffect(() => {
    savedCallback.current = callback;
  }, [callback]);

  // Setup interval
  useEffect(() => {
    // Não criar interval se delay null
    if (delay === null) {
      return;
    }

    const id = setInterval(() => {
      savedCallback.current(); // Chama versão atual do callback
    }, delay);

    // Cleanup
    return () => clearInterval(id);
  }, [delay]);
}

// Uso: Contador
function Counter() {
  const [count, setCount] = useState(0);
  const [isRunning, setIsRunning] = useState(true);

  useInterval(() => {
    setCount(count + 1); // Sempre tem valor atual
  }, isRunning ? 1000 : null); // null pausa interval

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => setIsRunning(!isRunning)}>
        {isRunning ? 'Pausar' : 'Iniciar'}
      </button>
      <button onClick={() => setCount(0)}>Reset</button>
    </div>
  );
}

// Uso: Timer countdown
function Countdown({ initialSeconds }) {
  const [seconds, setSeconds] = useState(initialSeconds);

  useInterval(() => {
    setSeconds(s => s > 0 ? s - 1 : 0);
  }, seconds > 0 ? 1000 : null); // Para quando chega em 0

  const minutes = Math.floor(seconds / 60);
  const secs = seconds % 60;

  return (
    <div>
      {minutes}:{secs.toString().padStart(2, '0')}
      {seconds === 0 && <p>Tempo esgotado!</p>}
    </div>
  );
}

// Uso: Polling de API
function LiveData() {
  const [data, setData] = useState(null);

  useInterval(async () => {
    const response = await fetch('/api/live-data');
    const json = await response.json();
    setData(json);
  }, 5000); // Poll a cada 5s

  return <div>Data: {JSON.stringify(data)}</div>;
}

// Uso: Animação
function ProgressBar({ duration }) {
  const [progress, setProgress] = useState(0);

  useInterval(() => {
    setProgress(p => {
      const next = p + 1;
      return next > 100 ? 100 : next;
    });
  }, duration / 100);

  return (
    <div className="progress-bar">
      <div
        className="progress-fill"
        style={{ width: `${progress}%` }}
      />
    </div>
  );
}
```

#### Variação: useInterval com Pause/Resume

```javascript
function useControllableInterval(callback, delay) {
  const savedCallback = useRef(callback);
  const [isPaused, setIsPaused] = useState(false);

  useEffect(() => {
    savedCallback.current = callback;
  }, [callback]);

  useEffect(() => {
    if (delay === null || isPaused) {
      return;
    }

    const id = setInterval(() => {
      savedCallback.current();
    }, delay);

    return () => clearInterval(id);
  }, [delay, isPaused]);

  const pause = useCallback(() => setIsPaused(true), []);
  const resume = useCallback(() => setIsPaused(false), []);
  const toggle = useCallback(() => setIsPaused(p => !p), []);

  return { pause, resume, toggle, isPaused };
}

// Uso
function Timer() {
  const [count, setCount] = useState(0);
  const { pause, resume, toggle, isPaused } = useControllableInterval(() => {
    setCount(c => c + 1);
  }, 1000);

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={toggle}>
        {isPaused ? 'Resume' : 'Pause'}
      </button>
    </div>
  );
}
```

### 8.4.2 useTimeout: setTimeout com Cleanup

```javascript
function useTimeout(callback, delay) {
  const savedCallback = useRef(callback);

  // Atualizar ref quando callback muda
  useEffect(() => {
    savedCallback.current = callback;
  }, [callback]);

  // Setup timeout
  useEffect(() => {
    // Não criar timeout se delay null
    if (delay === null) {
      return;
    }

    const id = setTimeout(() => {
      savedCallback.current();
    }, delay);

    // Cleanup
    return () => clearTimeout(id);
  }, [delay]);
}

// Uso: Notificação temporária
function Notification({ message, duration = 3000 }) {
  const [isVisible, setIsVisible] = useState(true);

  useTimeout(() => {
    setIsVisible(false);
  }, duration);

  if (!isVisible) return null;

  return (
    <div className="notification">
      {message}
    </div>
  );
}

// Uso: Debounced action
function SearchInput() {
  const [query, setQuery] = useState('');
  const [results, setResults] = useState([]);

  // Reset delay quando query muda
  useTimeout(() => {
    if (query) {
      fetch(`/api/search?q=${query}`)
        .then(res => res.json())
        .then(setResults);
    }
  }, query ? 500 : null);

  return (
    <div>
      <input
        value={query}
        onChange={(e) => setQuery(e.target.value)}
      />
      <ul>
        {results.map(r => <li key={r.id}>{r.name}</li>)}
      </ul>
    </div>
  );
}

// Uso: Redirect após login
function LoginSuccess() {
  const navigate = useNavigate();

  useTimeout(() => {
    navigate('/dashboard');
  }, 2000);

  return <div>Login bem-sucedido! Redirecionando...</div>;
}

// Uso: Auto-hide tooltip
function Tooltip({ text, children }) {
  const [isVisible, setIsVisible] = useState(false);

  useTimeout(() => {
    setIsVisible(false);
  }, isVisible ? 3000 : null);

  return (
    <div onMouseEnter={() => setIsVisible(true)}>
      {children}
      {isVisible && <div className="tooltip">{text}</div>}
    </div>
  );
}
```

#### Variação: useTimeout com Reset

```javascript
function useTimeoutWithReset(callback, delay) {
  const savedCallback = useRef(callback);
  const timeoutIdRef = useRef(null);

  useEffect(() => {
    savedCallback.current = callback;
  }, [callback]);

  const reset = useCallback(() => {
    if (timeoutIdRef.current) {
      clearTimeout(timeoutIdRef.current);
    }

    if (delay !== null) {
      timeoutIdRef.current = setTimeout(() => {
        savedCallback.current();
      }, delay);
    }
  }, [delay]);

  const clear = useCallback(() => {
    if (timeoutIdRef.current) {
      clearTimeout(timeoutIdRef.current);
      timeoutIdRef.current = null;
    }
  }, []);

  useEffect(() => {
    reset();
    return clear;
  }, [reset, clear]);

  return { reset, clear };
}

// Uso: Inactivity detector
function InactivityDetector({ onInactive, timeout = 60000 }) {
  const { reset } = useTimeoutWithReset(onInactive, timeout);

  useEventListener('mousemove', reset);
  useEventListener('keydown', reset);
  useEventListener('click', reset);

  return null; // Component invisível
}
```

### 8.4.3 useUpdateEffect: useEffect Apenas em Updates

```javascript
function useUpdateEffect(effect, deps) {
  const isFirstRender = useRef(true);

  useEffect(() => {
    // Skip na primeira render (mount)
    if (isFirstRender.current) {
      isFirstRender.current = false;
      return;
    }

    // Executar apenas em updates
    return effect();
  }, deps);
}

// Uso: Log apenas de mudanças
function Component({ userId }) {
  useUpdateEffect(() => {
    console.log('User ID mudou para:', userId);
    // Não loga no mount, apenas quando userId muda
  }, [userId]);

  return <div>User: {userId}</div>;
}

// Uso: Fetch apenas em updates
function UserProfile({ userId }) {
  const [user, setUser] = useState(null);

  // Fetch inicial (mount)
  useEffect(() => {
    fetch(`/api/users/${userId}`)
      .then(res => res.json())
      .then(setUser);
  }, []);

  // Re-fetch apenas quando userId muda (não no mount)
  useUpdateEffect(() => {
    fetch(`/api/users/${userId}`)
      .then(res => res.json())
      .then(setUser);
  }, [userId]);

  return <div>{user?.name}</div>;
}

// Uso: Animação em mudança
function AnimatedCounter({ count }) {
  const [isAnimating, setIsAnimating] = useState(false);

  useUpdateEffect(() => {
    setIsAnimating(true);
    const timer = setTimeout(() => setIsAnimating(false), 300);
    return () => clearTimeout(timer);
  }, [count]);

  return (
    <div className={isAnimating ? 'animating' : ''}>
      {count}
    </div>
  );
}

// Uso: Validação apenas após primeira edição
function FormField({ value, validate }) {
  const [error, setError] = useState(null);

  useUpdateEffect(() => {
    const validationError = validate(value);
    setError(validationError);
  }, [value, validate]);

  return (
    <div>
      <input value={value} />
      {error && <span className="error">{error}</span>}
    </div>
  );
}
```

### 8.4.4 useIsFirstRender: Detectar Primeira Render

```javascript
function useIsFirstRender() {
  const isFirstRender = useRef(true);

  useEffect(() => {
    isFirstRender.current = false;
  }, []);

  return isFirstRender.current;
}

// Uso: Lógica diferente em mount vs update
function Component({ data }) {
  const isFirstRender = useIsFirstRender();

  useEffect(() => {
    if (isFirstRender) {
      console.log('Componente montou');
      // Lógica de inicialização
    } else {
      console.log('Componente atualizou');
      // Lógica de atualização
    }
  }, [data, isFirstRender]);

  return <div>{data}</div>;
}

// Uso: Skip validação no mount
function Form() {
  const [email, setEmail] = useState('');
  const [error, setError] = useState(null);
  const isFirstRender = useIsFirstRender();

  useEffect(() => {
    if (isFirstRender) {
      return; // Não validar no mount
    }

    // Validar apenas após primeira edição
    if (!email.includes('@')) {
      setError('Email inválido');
    } else {
      setError(null);
    }
  }, [email, isFirstRender]);

  return (
    <div>
      <input value={email} onChange={(e) => setEmail(e.target.value)} />
      {error && <span>{error}</span>}
    </div>
  );
}

// Uso: Animação apenas em atualizações
function TransitionComponent({ content }) {
  const isFirstRender = useIsFirstRender();
  const [shouldAnimate, setShouldAnimate] = useState(false);

  useEffect(() => {
    if (!isFirstRender) {
      setShouldAnimate(true);
      const timer = setTimeout(() => setShouldAnimate(false), 500);
      return () => clearTimeout(timer);
    }
  }, [content, isFirstRender]);

  return (
    <div className={shouldAnimate ? 'fade-in' : ''}>
      {content}
    </div>
  );
}
```

#### Variação: useIsMounted

```javascript
function useIsMounted() {
  const isMounted = useRef(false);

  useEffect(() => {
    isMounted.current = true;

    return () => {
      isMounted.current = false;
    };
  }, []);

  return useCallback(() => isMounted.current, []);
}

// Uso: Prevenir setState em unmounted component
function Component() {
  const isMounted = useIsMounted();

  const fetchData = async () => {
    const response = await fetch('/api/data');
    const data = await response.json();

    // Apenas atualizar state se ainda mounted
    if (isMounted()) {
      setData(data);
    }
  };

  return <button onClick={fetchData}>Fetch</button>;
}
```

---

## 🎯 Aplicabilidade e Contextos

### Quando Usar Cada Hook

**useInterval:**
- Timers, cronômetros
- Polling de APIs
- Animações baseadas em tempo
- Progress bars

**useTimeout:**
- Notificações temporárias
- Redirects após ação
- Debounce manual
- Auto-hide de elementos

**useUpdateEffect:**
- Lógica apenas em mudanças (não mount)
- Validação após primeira edição
- Animações em transições
- Logs de mudanças

**useIsFirstRender:**
- Distinguir mount vs update
- Skip validação inicial
- Inicialização vs atualização

---

## ⚠️ Limitações e Considerações

### Stale Closures

**Problema resolvido pelos hooks:**
```javascript
// ❌ Sem useInterval
setInterval(() => {
  console.log(count); // Stale!
}, 1000);

// ✅ Com useInterval
useInterval(() => {
  console.log(count); // Sempre atual
}, 1000);
```

### Performance

**Interval/timeout têm custo:**
- CPU: Callback executa periodicamente
- Memória: Mantém closure ativa

**Otimização:** Pausar quando não visível (Page Visibility API).

### Cleanup Crítico

**Memory leaks sem cleanup:**
```javascript
// ❌ Sem cleanup
useEffect(() => {
  setInterval(() => console.log('tick'), 1000);
  // Interval nunca limpo!
}, []);

// ✅ Com cleanup
useEffect(() => {
  const id = setInterval(() => console.log('tick'), 1000);
  return () => clearInterval(id);
}, []);
```

---

## 🔗 Interconexões Conceituais

### Relação com useRef

Hooks de timing usam useRef para armazenar callbacks/IDs sem causar re-render.

### Relação com useEffect

useUpdateEffect e useIsFirstRender são wrappers de useEffect.

### Relação com Performance

Timers controlados evitam re-renders e execuções desnecessárias.

---

## 🚀 Evolução e Próximos Conceitos

### Progressão Natural

```
setInterval/setTimeout manuais
    ↓
Problemas (stale closures, cleanup)
    ↓
useInterval/useTimeout (abstrações)
    ↓
Composição (timers + outros hooks)
```

---

## 📚 Conclusão

Custom Hooks de Timing & Lifecycle são **essenciais** para código temporal robusto. Princípios:

1. **useInterval**: setInterval sem stale closures
2. **useTimeout**: setTimeout com cleanup automático
3. **useUpdateEffect**: Executar apenas em updates
4. **useIsFirstRender**: Detectar primeira render

**Recomendações:**
- Use useInterval para timers (evita stale closures)
- useTimeout com cleanup para ações temporárias
- useUpdateEffect para lógica apenas em mudanças
- useIsFirstRender para distinguir mount/update
- Sempre cleanup timers (evitar memory leaks)
- Refs para armazenar valores mutáveis

**Dominar hooks de timing/lifecycle = código temporal livre de bugs sutis.**
