# Ferramentas de Desenvolvimento

## 🎯 Introdução e Definição

### Definição Conceitual

**Ferramentas de Desenvolvimento** são **utilitários que melhoram produtividade** durante desenvolvimento, incluindo **DevTools, extensions, debuggers, profilers e generators**. Conceitualmente, representam **superpoderes do desenvolvedor** - transformam tasks manuais em automáticas, bugs invisíveis em visíveis e complexidade em simplicidade.

### Problema que Resolve

**Sem Ferramentas de Dev**:
- Debug com console.log
- Inspecionar state manualmente
- Escrever boilerplate repetitivo
- Performance issues invisíveis

**Com Ferramentas de Dev**:
- Debug visual e interativo
- State timeline e diff
- Geração automática de código
- Performance profiling detalhado

---

## 🔍 React DevTools

### Instalação

**Browser Extension**:
- [Chrome](https://chrome.google.com/webstore/detail/react-developer-tools)
- [Firefox](https://addons.mozilla.org/en-US/firefox/addon/react-devtools/)
- Edge (Chrome Web Store)

### Components Tab

```
Features:
├── Component tree visual
├── Props inspection
├── State inspection
├── Hooks inspection
├── Context values
├── Edit props/state ao vivo
└── Search components (Ctrl+F)
```

**Uso**:
1. Abrir DevTools (F12)
2. Tab "Components"
3. Clicar em componente → Ver props/state
4. Editar valores → Ver mudanças ao vivo

### Profiler Tab

```tsx
// ✅ Habilitar Profiler
function App() {
  return (
    <React.Profiler id="App" onRender={handleRender}>
      <Router />
    </React.Profiler>
  );
}

function handleRender(
  id: string,
  phase: 'mount' | 'update',
  actualDuration: number,
  baseDuration: number,
  startTime: number,
  commitTime: number
) {
  console.log(`${id} (${phase}) took ${actualDuration}ms`);
}
```

**Profiler Features**:
- Flame graph (visualizar renders)
- Ranked (componentes mais lentos)
- Commit timeline
- Identificar re-renders desnecessários

**Como Usar**:
1. Click "Record"
2. Interagir com app
3. Click "Stop"
4. Analisar flame graph

**Dicas**:
- Procurar barras amarelas/vermelhas (slow)
- "Highlight updates" → Ver re-renders ao vivo
- "Record why each component rendered"

---

## 🎨 VS Code Extensions

### Essenciais

```
1. ES7+ React/Redux/React-Native snippets
   - rfc → React Functional Component
   - rafce → Arrow function export
   - uef → useEffect
   - usf → useState

2. Prettier - Code formatter
   - Format on save
   - Consistent style

3. ESLint
   - Inline errors
   - Quick fixes

4. Auto Rename Tag
   - Renomeia tag de fechamento automaticamente

5. Color Highlight
   - Mostra cores no código (#ff0000)

6. Path Intellisense
   - Autocomplete de paths

7. Error Lens
   - Errors inline no código

8. GitLens
   - Git blame, history inline
```

### Snippets Customizados

```json
// .vscode/snippets.json
{
  "React Component": {
    "prefix": "rfc",
    "body": [
      "type ${1:Component}Props = {",
      "  $2",
      "};",
      "",
      "export function ${1:Component}({ $3 }: ${1:Component}Props) {",
      "  return (",
      "    <div>",
      "      $0",
      "    </div>",
      "  );",
      "}"
    ],
    "description": "React functional component with TypeScript"
  },

  "useState Hook": {
    "prefix": "usf",
    "body": [
      "const [$1, set${1/(.*)/${1:/capitalize}/}] = useState$2($3);"
    ],
    "description": "useState hook"
  },

  "useEffect Hook": {
    "prefix": "uef",
    "body": [
      "useEffect(() => {",
      "  $1",
      "  ",
      "  return () => {",
      "    $2",
      "  };",
      "}, [$3]);"
    ],
    "description": "useEffect with cleanup"
  }
}
```

---

## 🐛 Chrome DevTools

### Console Tricks

```tsx
// ✅ Console helpers
console.table(users); // Tabela bonita
console.time('fetch'); // Começar timer
console.timeEnd('fetch'); // Parar timer

console.group('User Info');
console.log('Name:', user.name);
console.log('Email:', user.email);
console.groupEnd();

// Debug condicional
console.assert(user.age > 18, 'User must be 18+');

// Trace call stack
console.trace('How did I get here?');
```

### Network Tab

**Features**:
- Ver todas as requests
- Filtrar por tipo (XHR, Fetch, JS, CSS)
- Ver headers, payload, response
- Throttle network (simulate 3G)
- Copiar como fetch/curl

**Uso**:
1. Filter: Fetch/XHR
2. Click request → Preview/Response
3. Throttle: Fast 3G (testar slow network)

### Performance Tab

**Features**:
- Record runtime performance
- FPS meter
- Main thread activity
- Identify bottlenecks

**Uso**:
1. Click Record
2. Interagir com app
3. Click Stop
4. Analisar timeline

---

## 📊 Redux DevTools

### Instalação

```bash
npm install @reduxjs/toolkit react-redux
npm install --save-dev @redux-devtools/extension
```

### Setup

```tsx
import { configureStore } from '@reduxjs/toolkit';
import { devToolsEnhancer } from '@redux-devtools/extension';

const store = configureStore({
  reducer: rootReducer,
  devTools: process.env.NODE_ENV !== 'production'
});
```

### Features

```
Features:
├── Action log (timeline)
├── State diff (antes/depois)
├── Time travel (jump to action)
├── Export/import state
├── Dispatch actions manualmente
└── Skip/revert actions
```

---

## 🔧 Code Generators

### Plop.js (Template Generator)

```bash
npm install --save-dev plop
```

```javascript
// plopfile.js
export default function (plop) {
  plop.setGenerator('component', {
    description: 'Create a React component',
    prompts: [
      {
        type: 'input',
        name: 'name',
        message: 'Component name:'
      }
    ],
    actions: [
      {
        type: 'add',
        path: 'src/components/{{pascalCase name}}/{{pascalCase name}}.tsx',
        templateFile: 'templates/component.tsx.hbs'
      },
      {
        type: 'add',
        path: 'src/components/{{pascalCase name}}/{{pascalCase name}}.test.tsx',
        templateFile: 'templates/component.test.tsx.hbs'
      },
      {
        type: 'add',
        path: 'src/components/{{pascalCase name}}/index.ts',
        template: 'export { {{pascalCase name}} } from \'./{{pascalCase name}}\';'
      }
    ]
  });
}
```

```handlebars
<!-- templates/component.tsx.hbs -->
type {{pascalCase name}}Props = {
  // TODO: Add props
};

export function {{pascalCase name}}(props: {{pascalCase name}}Props) {
  return (
    <div>
      {{pascalCase name}}
    </div>
  );
}
```

```bash
# Uso
npx plop component
# Component name: UserCard
# ✔ Created src/components/UserCard/UserCard.tsx
# ✔ Created src/components/UserCard/UserCard.test.tsx
# ✔ Created src/components/UserCard/index.ts
```

---

## 🎯 Storybook

### Instalação

```bash
npx storybook@latest init
```

### Component Story

```tsx
// Button.stories.tsx
import type { Meta, StoryObj } from '@storybook/react';
import { Button } from './Button';

const meta: Meta<typeof Button> = {
  title: 'Components/Button',
  component: Button,
  argTypes: {
    variant: {
      control: 'select',
      options: ['primary', 'secondary', 'danger']
    },
    size: {
      control: 'select',
      options: ['small', 'medium', 'large']
    }
  }
};

export default meta;
type Story = StoryObj<typeof Button>;

export const Primary: Story = {
  args: {
    children: 'Click me',
    variant: 'primary'
  }
};

export const Secondary: Story = {
  args: {
    children: 'Click me',
    variant: 'secondary'
  }
};

export const Disabled: Story = {
  args: {
    children: 'Disabled',
    disabled: true
  }
};
```

**Benefits**:
- Desenvolver componentes isoladamente
- Documentação visual
- Testar todos os estados
- Compartilhar com designers

---

## 🔍 React Query Devtools

### Setup

```tsx
import { ReactQueryDevtools } from '@tanstack/react-query-devtools';

function App() {
  return (
    <QueryClientProvider client={queryClient}>
      <Router />
      <ReactQueryDevtools initialIsOpen={false} />
    </QueryClientProvider>
  );
}
```

### Features

```
Features:
├── Ver todas as queries
├── Status (loading, success, error)
├── Data inspection
├── Cache invalidation manual
├── Refetch queries
└── Query timeline
```

---

## ⚡ Vite DevTools

### vite-plugin-inspect

```bash
npm install --save-dev vite-plugin-inspect
```

```typescript
// vite.config.ts
import { defineConfig } from 'vite';
import Inspect from 'vite-plugin-inspect';

export default defineConfig({
  plugins: [
    react(),
    Inspect() // http://localhost:3000/__inspect/
  ]
});
```

**Features**:
- Ver transformações de módulos
- Debug plugin pipeline
- Ver imports/exports

---

## 🎨 Styling DevTools

### TailwindCSS DevTools

```bash
npm install --save-dev @tailwindcss/inspector
```

**Features**:
- Inspector de classes Tailwind
- Hover → Ver classes aplicadas

### CSS DevTools (Chrome)

**Features**:
- Edit CSS ao vivo
- Ver computed styles
- Accessibility tree
- Color picker

---

## 🚀 Conclusão

Ferramentas de Desenvolvimento incluem:
- **React DevTools**: Components tree, Profiler
- **VS Code**: Extensions (ESLint, Prettier, snippets)
- **Chrome DevTools**: Console, Network, Performance
- **Redux DevTools**: Action log, time travel
- **Generators**: Plop.js para boilerplate
- **Storybook**: Component development isolado
- **React Query DevTools**: Query/mutation inspection

**Setup Recomendado**:
1. React DevTools (browser extension)
2. VS Code + extensions essenciais
3. Storybook (componentes isolados)
4. React Query DevTools (se usar React Query)

Resultado: Desenvolvimento 10x mais rápido, debugging 100x mais fácil.
