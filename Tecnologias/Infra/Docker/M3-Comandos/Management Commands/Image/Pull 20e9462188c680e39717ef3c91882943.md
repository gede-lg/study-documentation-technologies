# Pull

Olá, Gedê! Como A.R.I.A, estou aqui para te ajudar a dominar o Docker. Vamos detalhar o comando `docker image pull` para você.

O comando `docker image pull` (ou simplesmente `docker pull`) é a sua principal ferramenta para baixar imagens de contêineres de um *registry* (um repositório de imagens), como o Docker Hub. Pense nele como o `git clone` para imagens Docker.

---

### Sintaxe Geral

A estrutura básica do comando é a seguinte:

```bash
docker image pull [OPTIONS] NAME[:TAG|@DIGEST]

```

Vamos quebrar cada parte.

---

### Parâmetros Principais

Esses são os componentes que identificam a imagem que você deseja baixar.

| Parâmetro | Obrigatório | Descrição | Exemplo |
| --- | --- | --- | --- |
| `NAME` | Sim | É o nome da imagem. Geralmente, segue o formato `<usuário>/<nome_da_imagem>` (para imagens oficiais, como `ubuntu` ou `node`, o prefixo do usuário é omitido). | `ubuntu`, `mysql`, `bitnami/kafka` |
| `[:TAG]` | Não | É uma etiqueta que representa uma versão específica da imagem. Se você não especificar uma tag, o Docker usará a tag padrão, que é a **`latest`**. | `:22.04`, `:8.0`, `:lts-alpine` |
| `[@DIGEST]` | Não | É um identificador único e imutável (um hash `sha256`) para uma versão específica da imagem. Usar o digest garante que você está baixando exatamente a mesma imagem, byte por byte, independentemente da tag, que pode ser atualizada. | `@sha256:4dd97cef...` |

**Em resumo:** você **DEVE** fornecer o `NAME` da imagem e, opcionalmente, pode especificar uma `TAG` para uma versão ou um `DIGEST` para uma versão exata e imutável.

---

### Opções (Flags)

As opções modificam o comportamento do comando `pull`.

| Opção | Abreviação | Descrição | Exemplo de Uso |
| --- | --- | --- | --- |
| `--all-tags` | `-a` | Baixa **todas** as imagens com todas as tags disponíveis para o repositório especificado. Cuidado, isso pode consumir bastante espaço em disco. | `docker pull --all-tags redis` |
| `--disable-content-trust` |  | Desativa a verificação de assinatura da imagem. Por padrão, o Docker tenta verificar se a imagem é autêntica e não foi adulterada (se o Content Trust estiver habilitado). | `docker pull --disable-content-trust meu-repo/minha-imagem` |
| `--platform` |  | Especifica a arquitetura/plataforma da imagem a ser baixada, caso o *registry* ofereça suporte a múltiplas plataformas (ex: `linux/amd64`, `linux/arm64`). Isso é muito útil ao trabalhar em um Mac com chip M1/M2/M3 (ARM) e precisar de uma imagem para um servidor Linux (AMD64). | `docker pull --platform linux/amd64 ubuntu` |
| `--quiet` | `-q` | Suprime a saída detalhada do terminal. Em vez de mostrar o progresso do download de cada camada, ele exibe apenas o ID da imagem baixada no final. | `docker pull -q nginx` |

---

### Exemplos Práticos

Vamos ver como combinar tudo isso, Gedê.

1. **Baixar a imagem oficial do Go (a mais recente):**
    
    ```bash
    # O Docker assume a tag :latest por padrão
    docker pull golang
    
    ```
    
2. **Baixar uma versão específica do MySQL:**
    
    ```bash
    # Baixando a versão 8.0 do MySQL
    docker pull mysql:8.0
    
    ```
    
3. **Baixar uma imagem para uma arquitetura diferente:**
Como você está migrando para Go, talvez precise testar algo em uma arquitetura ARM, mesmo estando em um PC com processador Intel/AMD.
    
    ```bash
    # Baixa a imagem do Alpine Linux para a arquitetura ARM de 64 bits
    docker pull --platform linux/arm64 alpine
    
    ```
    
4. **Baixar uma imagem de forma silenciosa para usar em um script:**
    
    ```bash
    # O terminal não mostrará o progresso, apenas o ID final da imagem
    docker pull -q redis:alpine
    
    ```
    
5. **Garantir a imutabilidade usando o digest:**
Primeiro, você encontraria o digest no Docker Hub ou usando o comando `docker image inspect`.
    
    ```bash
    # Baixa a imagem exata referenciada por este hash, ignorando a tag
    docker pull nginx@sha256:e90ac5331fe0942a27e9a35476100343a4195430f812cb5a6234e45f778912e5
    
    ```
    

Espero que esta explicação detalhada tenha sido útil para você, Gedê! O `docker pull` é o ponto de partida para quase tudo que você fará com Docker. Se tiver mais alguma dúvida, é só chamar a A.R.I.A. aqui! 😉