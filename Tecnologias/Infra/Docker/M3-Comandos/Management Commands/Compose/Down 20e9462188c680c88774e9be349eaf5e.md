# Down

## Visão Geral do Comando

O comando `docker compose down` para e remove containers e redes, com opções para remover volumes e imagens.

## Sintaxe Básica

```
docker compose down [OPTIONS] [SERVICES]
```

## Parâmetros e Opções

| **Opção** | **Tipo** | **Descrição** |
| --- | --- | --- |
| --dry-run | boolean | Executa o comando em modo de simulação |
| --remove-orphans | boolean | Remove containers de serviços não definidos no arquivo Compose |
| --rmi | string | Remove imagens usadas pelos serviços ("local"|"all") |
| -t, --timeout | integer | Especifica tempo limite para desligamento em segundos |
| -v, --volumes | boolean | Remove volumes nomeados declarados na seção "volumes" e volumes anônimos anexados aos containers |

## Exemplos de Uso

```
# Parar e remover todos os containers e redes
docker compose down

# Parar e remover incluindo volumes
docker compose down -v

# Remover tudo, incluindo imagens
docker compose down --rmi all

# Parar com timeout personalizado
docker compose down --timeout 30

# Remover containers órfãos
docker compose down --remove-orphans

```

<aside>
⚠️ Importante: O comando down remove permanentemente os containers e redes. Use com cautela, especialmente com a opção -v que remove volumes com dados persistentes.

</aside>

<aside>
💡 Dica: A opção --rmi "local" remove apenas imagens sem tag personalizada, enquanto "all" remove todas as imagens utilizadas pelos serviços.

</aside>