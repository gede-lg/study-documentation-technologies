# Cp

## Visão Geral do Comando

O comando `docker compose cp` permite copiar arquivos ou pastas entre um container de serviço e o sistema de arquivos local.

## Sintaxe Básica

```
docker compose cp [OPTIONS] SERVICE:SRC_PATH DEST_PATH|-
docker compose cp [OPTIONS] SRC_PATH|- SERVICE:DEST_PATH
```

## Parâmetros e Opções

| **Opção** | **Tipo** | **Descrição** |
| --- | --- | --- |
| --all | boolean | Inclui containers criados pelo comando run |
| -a, --archive | boolean | Modo arquivo (copia todas as informações uid/gid) |
| --dry-run | boolean | Executa o comando em modo de simulação |
| -L, --follow-link | boolean | Sempre segue links simbólicos no SRC_PATH |
| --index | integer | Índice do container quando o serviço possui múltiplas réplicas |

## Exemplos de Uso

```
# Copiar arquivo do container para o host
docker compose cp web-service:/app/config.json ./local-config.json

# Copiar pasta do host para o container
docker compose cp ./local-data web-service:/app/data

# Copiar usando modo arquivo
docker compose cp -a web-service:/app/logs ./logs

# Copiar de container específico em serviço com múltiplas réplicas
docker compose cp --index=2 web-service:/app/data ./backup

# Copiar seguindo links simbólicos
docker compose cp -L web-service:/app/config ./config

```

<aside>
⚠️ Importante: O caractere '-' pode ser usado como DEST_PATH para enviar a saída para stdout ou como SRC_PATH para ler da stdin.

</aside>

<aside>
💡 Dica: Use a opção --archive para preservar permissões, timestamps e outros metadados durante a cópia.

</aside>