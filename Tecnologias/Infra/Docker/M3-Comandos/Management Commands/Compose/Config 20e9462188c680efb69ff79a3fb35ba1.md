# Config

## Visão Geral do Comando

O comando `docker compose config` analisa, resolve e renderiza o arquivo compose em formato canônico.

## Sintaxe Básica

```
docker compose config [OPTIONS] [SERVICE...]
```

## Parâmetros e Opções

| **Opção** | **Tipo** | **Descrição** |
| --- | --- | --- |
| --dry-run | boolean | Executa o comando em modo de simulação |
| --environment | boolean | Exibe o ambiente usado para interpolação |
| --format | string | Formata a saída (yaml | json) |
| --hash | string | Exibe o hash da configuração do serviço, um por linha |
| --images | boolean | Exibe os nomes das imagens, um por linha |
| --lock-image-digests | boolean | Produz um arquivo de override com digests das imagens |
| --no-consistency | boolean | Não verifica consistência do modelo (pode produzir saída Compose inválida) |
| --no-env-resolution | boolean | Não resolve arquivos env dos serviços |
| --no-interpolate | boolean | Não interpola variáveis de ambiente |
| --no-normalize | boolean | Não normaliza o modelo compose |
| --no-path-resolution | boolean | Não resolve caminhos de arquivo |
| -o, --output | string | Salva em arquivo (padrão: stdout) |
| --profiles | boolean | Exibe os nomes dos perfis, um por linha |
| -q, --quiet | boolean | Apenas valida a configuração, sem exibir nada |
| --resolve-image-digests | boolean | Fixa tags de imagem para digests |
| --services | boolean | Exibe os nomes dos serviços, um por linha |
| --variables | boolean | Exibe variáveis do modelo e valores padrão |
| --volumes | boolean | Exibe os nomes dos volumes, um por linha |

## Exemplos de Uso

```
# Validar e exibir configuração
docker compose config

# Exibir configuração em formato JSON
docker compose config --format json

# Listar apenas nomes dos serviços
docker compose config --services

# Salvar configuração em arquivo
docker compose config -o docker-compose.resolved.yml

# Exibir digests das imagens
docker compose config --resolve-image-digests

# Validar configuração silenciosamente
docker compose config -q

```

<aside>
⚠️ Importante: O comando config é útil para validar a configuração do compose e visualizar como o Docker Compose interpretará seu arquivo de configuração após resolver todas as variáveis e dependências.

</aside>

<aside>
💡 Dica: Use a opção --format para gerar saídas em formatos específicos, facilitando a integração com outras ferramentas.

</aside>