# Create

## Visão Geral do Comando

O comando `docker compose create` cria containers para um serviço sem iniciá-los.

## Sintaxe Básica

```
docker compose create [OPTIONS] [SERVICE...]
```

## Parâmetros e Opções

| **Opção** | **Tipo** | **Padrão** | **Descrição** |
| --- | --- | --- | --- |
| --build | boolean | false | Constrói imagens antes de criar os containers |
| --dry-run | boolean | false | Executa o comando em modo de simulação |
| --force-recreate | boolean | false | Recria containers mesmo sem mudanças na configuração ou imagem |
| --no-build | boolean | false | Não constrói imagem, mesmo se for política |
| --no-recreate | boolean | false | Não recria containers se já existirem |
| --pull | string | "policy" | Pull de imagem antes de executar ("always"|"missing"|"never"|"build") |
| --quiet-pull | boolean | false | Pull sem exibir informações de progresso |
| --remove-orphans | boolean | false | Remove containers de serviços não definidos no arquivo Compose |
| --scale | string | - | Escala SERVIÇO para N instâncias |
| -y, --yes | boolean | false | Assume "yes" como resposta para todos os prompts |

## Exemplos de Uso

```
# Criar containers para todos os serviços
docker compose create

# Criar containers com build prévio
docker compose create --build web-service

# Criar containers forçando recriação
docker compose create --force-recreate database

# Criar containers com escala específica
docker compose create --scale web=3

# Criar containers sem interação
docker compose create -y

```

<aside>
⚠️ Importante: O comando create apenas cria os containers, mas não os inicia. Use o comando start para iniciar os containers após criá-los.

</aside>

<aside>
💡 Dica: As opções --force-recreate e --no-recreate são mutuamente exclusivas e não podem ser usadas juntas.

</aside>