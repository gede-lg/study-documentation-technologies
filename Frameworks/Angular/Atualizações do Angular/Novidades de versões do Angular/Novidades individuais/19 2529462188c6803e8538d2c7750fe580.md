# 19

Beleza! Aqui vai um panorama **versão-por-versão do Angular 19** (19.0, 19.1 e 19.2), focado no que é realmente relevante para devs — com **status (estável vs. prévia/experimental)** e notas de implementação.

---

# Angular 19.0 (Nov 19, 2024)

## Estáveis (para uso em produção)

- **Reatividade “Signal Components”: estabilização oficial**
    - **`input()` (signals)**, **`output()` (função)**, **consultas de view/conteúdo como *signals*** (`viewChild`, `viewChildren`, `contentChild`, `contentChildren`) — agora “production-ready”, com **migrations** dedicadas:
        - `ng generate @angular/core:signal-input-migration`
        - `ng generate @angular/core:output-migration`
        - `ng generate @angular/core:signal-queries-migration`. ([Angular Blog](https://blog.angular.dev/meet-angular-v19-7b29dfd05b84), [angular.dev](https://angular.dev/reference/migrations/signal-inputs?utm_source=chatgpt.com))
    - **`model()` (two-way via signals)**: estabilizado no 19 segundo cobertura técnica da Ninja-Squad (inclui `model()` junto com input/output/queries). Útil para bindings 2-vias sem `EventEmitter`. ([Ninja Squad](https://blog.ninja-squad.com/2024/11/19/what-is-new-angular-19.0/))
        
        > Dica rápida: active = model(false) cria implicitamente o activeChange para [(active)].
        > 
- **Event Replay**: **agora estável e habilitado por padrão** em apps com SSR novos (reproduz cliques/inputs ocorridos antes da hidratação). ([Angular Blog](https://blog.angular.dev/meet-angular-v19-7b29dfd05b84))
- **Standalone por padrão + enforcement opcional**
    - **Standalone = `true` por padrão** (schematic remove `standalone: true` dos seus componentes e define `false` quando necessário).
    - **`"strictStandalone": true`** no `angularCompilerOptions` gera erro se detectar entidades não-standalone. ([Angular Blog](https://blog.angular.dev/meet-angular-v19-7b29dfd05b84))
- **Variáveis locais em template (`@let`)**: saiu de *preview* (v18.1) e **virou estável** no 19. ([Angular Blog](https://blog.angular.dev/meet-angular-v19-7b29dfd05b84))
- **HMR mais rápido (CLI)**
    - **HMR para estilos**: **ativado por padrão**.
    - HMR para **templates** atrás de *flag* de ambiente (detalhes abaixo em 19.1, onde vira default). ([Angular Blog](https://blog.angular.dev/meet-angular-v19-7b29dfd05b84))
- **Deprecação importante — inicializadores**
    - **`APP_INITIALIZER`** (e também `ENVIRONMENT_INITIALIZER`/`PLATFORM_INITIALIZER`) **deprecados** em favor de **`provideAppInitializer`** (e pares `provideEnvironmentInitializer`/`providePlatformInitializer`). A função roda **em contexto de injeção**, então você pode usar `inject()` dentro do *initializer*. ([angular.dev](https://angular.dev/api/core/provideAppInitializer?utm_source=chatgpt.com), [Ninja Squad](https://blog.ninja-squad.com/2024/11/19/what-is-new-angular-19.0/?utm_source=chatgpt.com))
    - Ex.:
        
        ```tsx
        bootstrapApplication(App, {
          providers: [
            provideAppInitializer(() => inject(InitService).init())
          ]
        });
        
        ```
        
- **DX/Qualidade de vida (CLI/Material/CDK)**
    - **Diagnóstico de imports standalone não usados** (warning + *quick-fix* na Language Service).
    - **`ng build --define`** para injetar variáveis de ambiente em tempo de build.
    - **Material**: **novo Time Picker**; **Sass component overrides**; **CDK**: drag-and-drop bidimensional e **reordenar abas**. ([Angular Blog](https://blog.angular.dev/meet-angular-v19-7b29dfd05b84))

## Em *Developer Preview* / Experimental

- **Hidratação incremental (SSR)**
    
    Hidrata trechos do app sob demanda com a sintaxe **`@defer (hydrate on …)`**; habilite com `provideClientHydration(withIncrementalHydration())`. **Prévia** no 19.0; docs oficiais trazem *how-to* e motivação. ([Angular Blog](https://blog.angular.dev/meet-angular-v19-7b29dfd05b84), [angular.dev](https://angular.dev/guide/incremental-hydration?utm_source=chatgpt.com))
    
- **Render Mode por rota (híbrido SSR/SSG/CSR)**
    
    Configure **por rota** se renderiza no **Server**, **Prerender** (SSG) ou **Client** com `ServerRoute[]` (ex.: login no server, dashboard no client, demais prerender). **Prévia**. ([Angular Blog](https://blog.angular.dev/meet-angular-v19-7b29dfd05b84))
    
    ```tsx
    export const serverRouteConfig: ServerRoute[] = [
      { path: '/login', mode: RenderMode.Server },
      { path: '/dashboard', mode: RenderMode.Client },
      { path: '/**', mode: RenderMode.Prerender },
    ];
    
    ```
    
- **Zoneless no SSR**: experimentos para SSR sem `zone.js`, com *primitives* no `HttpClient`/Router para sinalizar “pronto para enviar”. ([Angular Blog](https://blog.angular.dev/meet-angular-v19-7b29dfd05b84))
- **Novas primitivas de reatividade**
    - **`linkedSignal()`**: *writable* que se “reancora” a um *computed* de origem quando essa origem muda (ideal p/ seleção que reseta ao trocar a lista). **Experimental**. ([Angular Blog](https://blog.angular.dev/meet-angular-v19-7b29dfd05b84))
    - **`resource()` / `rxResource()`**: base para **reatividade assíncrona** (carregar dados com *signals*). No 19.0 estreou como **experimental** e evolui em 19.2 (abaixo). ([Angular Blog](https://blog.angular.dev/meet-angular-v19-7b29dfd05b84))
- **Auto-CSP (hash-based CSP) no *builder***: geração automática de CSP por *hash* dos scripts inline — **opt-in**, **prévia**. ([Angular Blog](https://blog.angular.dev/meet-angular-v19-7b29dfd05b84))

---

# Angular 19.1 (Jan 2025)

## Estáveis

- **TypeScript 5.7** suportado (amplia a janela de compatibilidade do 19). ([Ninja Squad](https://blog.ninja-squad.com/2025/01/15/what-is-new-angular-19.1/))
- **HMR de templates agora por padrão** (não exige mais `NG_HMR_TEMPLATES=1`). ([Ninja Squad](https://blog.ninja-squad.com/2025/01/15/what-is-new-angular-19.1/))
- **`NgComponentOutlet`**: passa a expor `componentInstance` e pode ser exportado/recuperado no template. ([Ninja Squad](https://blog.ninja-squad.com/2025/01/15/what-is-new-angular-19.1/))
- **DevTools**: melhorias no **gráfico de rotas** e utilitários para **depurar sinais** (ex.: `getSignalGraph`), facilitando inspeção de reatividade. ([Ninja Squad](https://blog.ninja-squad.com/2025/01/15/what-is-new-angular-19.1/))

## Em *Developer Preview* / Experimental

- **SSR + i18n**:
    - **`subPath`** como substituto de `baseHref` para *apps* i18n com SSR.
    - **Redireciono para localidade preferida** (suporte no SSR). ([Ninja Squad](https://blog.ninja-squad.com/2025/01/15/what-is-new-angular-19.1/))
- **Performance de navegação SSR**: **preload de rotas lazy** via `modulepreload`. ([Ninja Squad](https://blog.ninja-squad.com/2025/01/15/what-is-new-angular-19.1/))
- **Empacotamento/builder**: **novo `ng-packagr` builder** (prévia). ([Ninja Squad](https://blog.ninja-squad.com/2025/01/15/what-is-new-angular-19.1/))
- **CLI**: aviso quando `localize` é importado de forma incorreta. ([Ninja Squad](https://blog.ninja-squad.com/2025/01/15/what-is-new-angular-19.1/))

---

# Angular 19.2 (Mar 5, 2025)

## Estáveis

- **Ergonomia de template**: ***untagged template literals*** nas expressões de template (ex.: `[class]="`layout col-${colWidth}`"`).
    
    **Forms**: validações/controles agora suportam **`Set`** (e outras melhorias).
    
    **Migrations**: conversão para **tags auto-fechadas** e diagnósticos extras (inclui *skip hydration diagnostic*). ([Angular Blog](https://blog.angular.dev/angular-19-2-is-now-available-673ec70aea12))
    
- **Compatibilidade**: **TypeScript 5.8** passa a ser suportado no ramo 19.2. ([Angular.love](https://angular.love/angular-19-2-whats-new?utm_source=chatgpt.com))

## Em *Developer Preview* / Experimental

- **Reatividade assíncrona — grande avanço do 19.2**
    - **`httpResource()`**: API reativa e declarativa para **fazer requisições HTTP** acompanhando *signals* (usa `HttpClient`, respeita interceptors).
    - **`rxResource()` com *streaming***: suporte a **múltiplos valores** (stream) e *resource streaming*.
    - **`resource()`**: base *promise-first* para recursos assíncronos; 19.2 traz melhorias (ex.: *default value* e comportamento de *streaming* via `rxResource`).
        
        Tudo **experimental**, com **RFC pública** aberta. ([Angular Blog](https://blog.angular.dev/angular-19-2-is-now-available-673ec70aea12))
        

---

## Notas de implementação (pitfalls & dicas rápidas)

- **Sinais em inputs/outputs/queries**
    - *Inputs* como *signals* são **somente leitura** no lado do filho; derive estado com `computed()`/`linkedSignal()` ou promova valores via `model()` quando precisar *write-back*. Migrations oficiais ajudam, mas **setters de `@Input()`** e casos com *type narrowing* dentro de `@if` podem exigir ajuste manual. ([Angular Blog](https://blog.angular.dev/meet-angular-v19-7b29dfd05b84))
- **`model()` x `input()`**
    - Use **`model()`** quando quiser **2-vias** e o **evento `FooChange`** implícito;
        
        use **`input()`** quando quiser **one-way + derivação** no filho. (Estabilizado no 19 conforme Ninja-Squad). ([Ninja Squad](https://blog.ninja-squad.com/2024/11/19/what-is-new-angular-19.0/))
        
- **Event Replay + Hidratação**
    - Em SSR, **mantenha `withEventReplay()`** na configuração de hidratação para não perder interações de usuário antes da hidratação. Combine com **hidratação incremental** quando usar `@defer`. ([Angular Blog](https://blog.angular.dev/meet-angular-v19-7b29dfd05b84))
- **Inicializadores**
    - Migre para **`provideAppInitializer`**; como roda em **contexto de injeção**, você pode chamar `inject()` direto no *initializer* (sem `deps`). ([angular.dev](https://angular.dev/api/core/provideAppInitializer?utm_source=chatgpt.com))
- **HMR**
    - 19.0: estilos por padrão; 19.1: **templates por padrão** — acelera o ciclo salvar-ver sem perder estado. ([Ninja Squad](https://blog.ninja-squad.com/2025/01/15/what-is-new-angular-19.1/))

---

Se quiser, eu monto um **checklist de migração focado no 19** (comandos, flags e pontos de verificação por módulo/área do projeto) — é só dizer 👍