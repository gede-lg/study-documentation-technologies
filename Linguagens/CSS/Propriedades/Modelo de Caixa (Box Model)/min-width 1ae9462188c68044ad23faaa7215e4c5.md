# min-width

A propriedade **`min-width`** define a **largura mínima** que um elemento pode ter. Se o conteúdo ou o tamanho do elemento for menor que esse valor, ele será forçado a ter no mínimo a largura definida.

---

## **📌 Sintaxe**

```css
element {
  min-width: valor;
}

```

📌 **Valores possíveis:**

- **`auto`** → O elemento se ajusta automaticamente (padrão).
- **Comprimento fixo** (ex: `300px`, `50rem`) → Define um tamanho mínimo absoluto.
- **Porcentagem** (ex: `80%`) → Baseia-se no elemento pai.
- **`inherit`** → Herda do elemento pai.
- **`initial`** → Reseta para o valor padrão (`auto`).
- **`unset`** → Reseta para o padrão ou herda.

---

## **📌 Exemplos de Uso**

### **1️⃣ Evitar que um Elemento Fique Muito Pequeno**

```css
.button {
  min-width: 120px;
  padding: 10px;
  background-color: blue;
  color: white;
  text-align: center;
}

```

🔹 O botão sempre terá **pelo menos 120px de largura**, mesmo se o texto for curto.

---

### **2️⃣ Criar um Layout Responsivo**

```css
.sidebar {
  min-width: 300px;
  width: 20%;
  background-color: lightgray;
}

```

🔹 A sidebar terá no mínimo **300px**, mas pode ser maior caso `width: 20%` resulte em um valor maior.

---

### **3️⃣ Garantir que Campos de Formulário Não Encolham Demais**

```css
input {
  min-width: 200px;
  width: 100%;
}

```

🔹 O campo de entrada terá **pelo menos 200px**, mas poderá crescer conforme o contêiner.

---

## **📌 `min-width` vs. `width` vs. `max-width`**

| Propriedade | Comportamento |
| --- | --- |
| `width: 400px;` | O elemento **sempre** terá **400px** de largura, independente do espaço disponível. |
| `min-width: 400px;` | O elemento pode crescer **acima** de 400px, mas nunca ficará menor que isso. |
| `max-width: 400px;` | O elemento pode ser menor que 400px, mas nunca **maior** que esse valor. |

---

## **📌 Quando Usar?**

✅ Para evitar que elementos fiquem **muito pequenos**.

✅ Para criar **layouts flexíveis** sem tamanhos fixos.

✅ Para tornar **botões, inputs e contêineres responsivos** e legíveis.

🚀 **Conclusão:** `min-width` é essencial no design responsivo para impedir que elementos fiquem menores do que o desejado, garantindo uma boa usabilidade!