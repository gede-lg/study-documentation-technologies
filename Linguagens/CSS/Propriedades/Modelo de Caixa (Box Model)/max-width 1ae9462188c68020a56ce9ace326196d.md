# max-width

A propriedade **`max-width`** define a largura máxima de um elemento. Se o conteúdo ou o tamanho do elemento for maior que esse valor, ele será ajustado para não ultrapassar esse limite.

---

## **📌 Sintaxe**

```css
element {
  max-width: valor;
}

```

📌 **Valores possíveis:**

- **`none`** (padrão) → Sem limite de largura.
- **Comprimento fixo** (ex: `300px`, `50rem`) → Define um limite absoluto.
- **Porcentagem** (ex: `80%`) → Baseia-se no elemento pai.
- **`inherit`** → Herda do elemento pai.
- **`initial`** → Reseta para o valor padrão (`none`).
- **`unset`** → Reseta para o valor padrão ou herda.

---

## **📌 Exemplos de Uso**

### **1️⃣ Garantir que um elemento não ultrapasse um tamanho máximo**

```css
.container {
  max-width: 1200px;
  margin: 0 auto; /* Centraliza */
  padding: 20px;
}

```

🔹 Se a tela for maior que **1200px**, o elemento não aumentará além desse tamanho.

🔹 Se a tela for menor, ele se ajustará automaticamente.

---

### **2️⃣ Tornar Imagens Responsivas**

```css
img {
  max-width: 100%;
  height: auto;
}

```

🔹 Evita que a imagem ultrapasse o tamanho do contêiner.

🔹 Mantém a proporção da imagem.

---

### **3️⃣ Criar um Layout Responsivo**

```css
.box {
  width: 100%;
  max-width: 600px;
  background-color: lightblue;
  padding: 20px;
}

```

🔹 O elemento pode se expandir até **600px**, mas nunca ultrapassará esse valor.

---

## **📌 `max-width` vs. `width`**

| Propriedade | Comportamento |
| --- | --- |
| `width: 600px;` | Sempre mantém **600px**, mesmo se a tela for menor. |
| `max-width: 600px;` | **Adapta-se** se a tela for menor que **600px**, tornando o layout responsivo. |

---

## **📌 Quando Usar?**

✅ Para evitar que elementos fiquem **maiores que um certo valor**.

✅ Para tornar layouts e imagens **mais flexíveis** e **responsivos**.

✅ Para combinar com `width: 100%` e garantir que elementos não ultrapassem um tamanho máximo.

🚀 **Conclusão:** `max-width` é essencial no design responsivo para limitar larguras sem forçar tamanhos fixos!