# min-height

A propriedade **`min-height`** define a **altura mínima** que um elemento pode ter. Se o conteúdo ou o tamanho do elemento for menor que esse valor, ele será forçado a ter pelo menos essa altura.

---

## **📌 Sintaxe**

```css
element {
  min-height: valor;
}

```

📌 **Valores possíveis:**

- **`auto`** → Ajusta-se automaticamente ao conteúdo (padrão).
- **Comprimento fixo** (ex: `300px`, `50vh`) → Define um tamanho mínimo absoluto.
- **Porcentagem** (ex: `80%`) → Baseia-se no elemento pai.
- **`inherit`** → Herda do elemento pai.
- **`initial`** → Reseta para o valor padrão (`auto`).
- **`unset`** → Reseta para o padrão ou herda.

---

## **📌 Exemplos de Uso**

### **1️⃣ Evitar que um Contêiner Fique Muito Pequeno**

```css
.container {
  min-height: 400px;
  background-color: lightgray;
  padding: 20px;
}

```

🔹 O contêiner sempre terá pelo menos **400px de altura**, mas pode crescer se o conteúdo for maior.

---

### **2️⃣ Criar uma Seção de Tela Inteira (`100vh`)**

```css
.hero-section {
  min-height: 100vh;
  background-image: url('fundo.jpg');
  background-size: cover;
  display: flex;
  align-items: center;
  justify-content: center;
}

```

🔹 Garante que a seção sempre ocupe **pelo menos toda a tela**, mesmo se o conteúdo for pequeno.

---

### **3️⃣ Evitar que um Campo de Texto Fique Muito Pequeno**

```css
textarea {
  min-height: 150px;
  width: 100%;
}

```

🔹 O campo de texto começará com **150px de altura**, mas poderá crescer se necessário.

---

## **📌 `min-height` vs. `height` vs. `max-height`**

| Propriedade | Comportamento |
| --- | --- |
| `height: 400px;` | O elemento **sempre** terá **400px** de altura, independente do conteúdo. |
| `min-height: 400px;` | O elemento pode crescer **acima** de 400px, mas nunca ficará menor. |
| `max-height: 400px;` | O elemento pode ser menor que 400px, mas nunca **maior** que esse valor. |

---

## **📌 Quando Usar?**

✅ Para garantir que um elemento **não fique menor** do que um valor mínimo.

✅ Para criar **seções flexíveis** que crescem conforme o conteúdo.

✅ Para melhorar a usabilidade de **textareas, cards, contêineres e layouts responsivos**.

🚀 **Conclusão:** `min-height` é essencial no design responsivo para impedir que elementos fiquem pequenos demais, garantindo uma melhor experiência para o usuário!