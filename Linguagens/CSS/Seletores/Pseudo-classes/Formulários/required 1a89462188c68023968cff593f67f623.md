# :required

---

## **1. Introdução**

A pseudo-classe **`:required`** é usada para selecionar elementos de formulário HTML que possuem o atributo `required`, indicando que um campo é obrigatório para envio do formulário.

**Relevância**:

- Melhora a **experiência do usuário** ao destacar campos obrigatórios.
- Facilita a **validação visual** de formulários, complementando a validação nativa do HTML5.

**Conceitos Fundamentais**:

- **Pseudo-classe**: Um seletor que define o estilo de um elemento em um **estado específico**.
- **HTML5 Validation**: Integração com atributos como `required`, `pattern`, e `type` para validação nativa.

---

## **2. Sumário**

1. Sintaxe e uso básico
2. Elementos compatíveis
3. Interação com outras pseudo-classes
4. Restrições e boas práticas
5. Exemplos de código
6. Informações complementares

---

## **3. Conteúdo Detalhado**

### **Sintaxe e Estrutura**

```css
seletor:required {
  propriedade: valor;
}

```

- **Exemplo**:
    
    ```css
    input:required {
      border: 2px solid #ff0000;
    }
    
    ```
    

### **Componentes Principais**

- **Elementos Compatíveis**:
    - `<input>` (text, email, number, etc.)
    - `<select>`
    - `<textarea>`
- **Interação com Outras Pseudo-classes**:
    - `:optional`: Elementos **não obrigatórios**.
    - `:valid`/`:invalid`: Estados de validação do campo.

### **Restrições**

1. **Navegadores Antigos**: Suporte limitado em versões do IE < 10.
2. **Estilização Limitada**: Alguns componentes (como `<select>`) podem não aceitar todos os estilos.

---

## **4. Exemplos de Código**

### **Exemplo Básico**

```html
<label for="email">E-mail *</label>
<input type="email" id="email" required>

```

```css
/* Estiliza campos obrigatórios */
input:required {
  background-color: #fff3f3;
  border-left: 4px solid #ff0000;
}

```

### **Exemplo Avançado (Combinação de Estados)**

```css
/* Campo obrigatório e inválido */
input:required:invalid {
  border-color: #ff0000;
}

/* Campo obrigatório e válido */
input:required:valid {
  border-color: #00ff00;
}

```

---

## **5. Informações Adicionais**

- **Acessibilidade**: Use ícones ou texto (como *) junto com cores para indicar campos obrigatórios.
- **JavaScript**: Combine com eventos como `input` ou `change` para validação dinâmica.
- **Boas Práticas**:
    - Não dependa apenas de cor para sinalizar obrigatoriedade.
    - Teste em múltiplos navegadores.

---

## **6. Referências para Estudo**

1. **MDN Web Docs**: [Pseudo-class :required](https://developer.mozilla.org/pt-BR/docs/Web/CSS/:required)
2. **CSS Tricks**: [Form Validation UX](https://css-tricks.com/form-validation-ux-html-css/)
3. **W3C**: [HTML5 Constraint Validation](https://www.w3.org/TR/html5/forms.html#constraint-validation)

---

**Formatado para clareza e aplicação prática!** 🚀