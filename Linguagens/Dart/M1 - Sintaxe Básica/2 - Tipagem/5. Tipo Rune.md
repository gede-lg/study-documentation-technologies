### Tipo Runes no Dart

#### O que é e para que serve?

No Dart, `Runes` representam uma sequência de valores Unicode, onde cada valor Unicode é um código de ponto Unicode. O tipo `Runes` é útil quando você precisa manipular strings no nível dos códigos de ponto Unicode, especialmente para trabalhar com caracteres especiais ou emojis, que podem não ser representados por um único caractere UTF-16. 

#### Sintaxe de uso

Para utilizar `Runes` no Dart, você pode criar uma sequência de `Runes` diretamente a partir de uma string. A classe `Runes` permite iterar sobre cada código de ponto Unicode na string. Aqui está um exemplo básico:

```dart
void main() {
  String text = 'Olá, mundo! 🌍';
  Runes runes = text.runes;

  runes.forEach((int rune) {
    var character = String.fromCharCode(rune);
    print(character);
  });
}
```

No exemplo acima, a string `text` é convertida em `Runes` e iterada, exibindo cada caractere, incluindo o emoji.

#### Restrições de uso

Os `Runes` representam valores de código de ponto Unicode que vão de 0 a 0x10FFFF. Isso cobre toda a faixa de códigos de ponto Unicode, mas é importante notar que alguns caracteres podem ser compostos por múltiplos códigos de ponto (caracteres combinados). 

Outro ponto a considerar é que `Runes` são úteis principalmente quando você precisa lidar com a representação interna dos caracteres, como ao trabalhar com emojis ou caracteres fora do BMP (Basic Multilingual Plane).

#### Conversões

##### De string para `Runes`

Como mostrado anteriormente, você pode converter uma string para `Runes` usando a propriedade `.runes`.

```dart
String text = 'Olá, mundo! 🌍';
Runes runes = text.runes;
```

##### De `Runes` para string

Para converter `Runes` de volta para uma string, você pode usar o construtor `String.fromCharCodes()`:

```dart
String fromRunes = String.fromCharCodes(runes);
print(fromRunes); // Saída: Olá, mundo! 🌍
```

##### De `Runes` para `List<int>`

Se você precisar de uma lista dos códigos de ponto Unicode, pode convertê-los diretamente:

```dart
List<int> codePoints = runes.toList();
print(codePoints); // Saída: [79, 108, 225, 44, 32, 109, 117, 110, 100, 111, 33, 32, 127757]
```

#### Quando utilizar?

Utilize `Runes` quando:

1. **Trabalhar com Emojis e Caracteres Especiais**: Se seu aplicativo manipula emojis ou caracteres que não estão no BMP, `Runes` são essenciais.
2. **Análise de Texto**: Quando precisar analisar e manipular texto no nível de código de ponto Unicode.
3. **Internacionalização**: Ao lidar com múltiplos idiomas e scripts, onde a representação de caracteres pode variar significativamente.

#### Exemplo Completo

Aqui está um exemplo mais completo que mostra algumas operações comuns com `Runes`:

```dart
void main() {
  String text = 'Dart é incrível! 🚀';

  // Convertendo a string para Runes
  Runes runes = text.runes;

  // Iterando sobre os runes
  runes.forEach((int rune) {
    var character = String.fromCharCode(rune);
    print('Rune: $rune, Character: $character');
  });

  // Convertendo de Runes para List<int>
  List<int> codePoints = runes.toList();
  print('Code points: $codePoints');

  // Convertendo de List<int> para string
  String fromCodePoints = String.fromCharCodes(codePoints);
  print('String from code points: $fromCodePoints');

  // Trabalhando com emojis e caracteres especiais
  String emojiText = '👋🌍';
  Runes emojiRunes = emojiText.runes;

  emojiRunes.forEach((int rune) {
    print('Emoji rune: $rune, Character: ${String.fromCharCode(rune)}');
  });
}
```

### Conclusão

O tipo `Runes` no Dart é uma ferramenta poderosa para manipular strings no nível de código de ponto Unicode, permitindo que você trabalhe com caracteres especiais, emojis e outros caracteres complexos de forma eficiente. Compreender e utilizar `Runes` é essencial para aplicações que exigem manipulação avançada de texto e suporte a internacionalização.